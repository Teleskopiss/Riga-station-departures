name: Scrape Arrivals Schedule

on:
  schedule:
    - cron: '30 * * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  scrape-arrivals-schedule:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r scraper/requirements.txt

      - name: Configure git
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run arrivals schedule scraper
        run: python scraper/scrape_arrivals_schedule.py

      - name: Commit and push
        run: |
          git add docs/full-day-arrivals.json
          git diff --cached --quiet && echo "No changes" || (git commit -m "chore: update arrivals schedule [skip ci]" && git pull --rebase origin main && git push)
